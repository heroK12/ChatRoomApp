<h1>Room一覧</h1>
<% @rooms.each do |room| %>
  <h2><%= link_to  room.title, room_path(room) %></h2>
  <p><%= room.text %></p>
  <%= link_to  "削除する", room_path(room), method: :delete, data: {confirm: "本当によろしいですか？"} %>
<% end %>